# @package _global_
# Tester pipeline: 25600 random Cyclomedia pairs with parking-preference prompt.

defaults:
  - _self_
  - override /hydra/launcher: slurm_monitor
  - override /data: cyclomedia_pairwise_manhattan_2025_1
  - override /prompt: pairwise_parking_ordinal
  - override /model: vllm_multimodal_qwen3_vl_2b

runtime:
  debug: false
  sample_n: null
  skip_inference: false
  job_memory_gb: 64

pair_sampler:
  pair_seed: 777
  max_pairs: 25600
  allow_replacement: false
  counterbalance_mode: balanced
  repeat_count: 2560
  repeat_fraction: 0.0

pipeline:
  output_root: ${runtime.output_root}
  allow_partial: false
  graph:
    nodes:
      pairwise:
        stage: pairwise_vqa
        depends_on: []
        outputs:
          results: outputs/pairwise/parking_tester_midsize_${now:%Y%m%d_%H%M%S}.parquet
        overrides:
          runtime.debug: ${runtime.debug}
          runtime.sample_n: ${runtime.sample_n}
          runtime.skip_inference: ${runtime.skip_inference}
          pair_sampler.pair_seed: ${pair_sampler.pair_seed}
          pair_sampler.max_pairs: ${pair_sampler.max_pairs}
          pair_sampler.allow_replacement: ${pair_sampler.allow_replacement}
          pair_sampler.counterbalance_mode: ${pair_sampler.counterbalance_mode}
          pair_sampler.repeat_count: ${pair_sampler.repeat_count}
          pair_sampler.repeat_fraction: ${pair_sampler.repeat_fraction}
          model.model_source: ${model.model_source}
          model.engine_kwargs.gpu_memory_utilization: 0.92
          model.engine_kwargs.tensor_parallel_size: 1
          model.batch_size: 16
          model.concurrency: 1
          sampling_params_vqa.max_tokens: 16
        launcher: slurm_gpu_2x
        wandb_suffix: pairwise_parking_tester_midsize

